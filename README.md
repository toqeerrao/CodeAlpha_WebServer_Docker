# CodeAlpha_WebServer_Docker
# Web Server using Docker

## Project Description
This project demonstrates how to run a simple web server using Docker and Nginx container.

## Docker Command Used
```bash
docker run -d -p 8080:80 --name myweb nginx
